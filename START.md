# Start CIE

Required environment variables:

```env
OPENAI_API_KEY=
MASTRA_PLATFORM_ACCESS_TOKEN=
MASTRA_PROJECT_ID=
```

Start the backend:

```bash
cd cie
yarn start:dev
```

Start the frontend in another terminal:

```bash
cd frontend
yarn start
```
